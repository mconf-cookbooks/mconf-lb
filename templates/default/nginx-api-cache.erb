<% node['mconf-lb']['nginx']['api_cache'].each do |item| %>
location <%= item[0] %> {
  proxy_cache api;
  proxy_cache_valid 200 <%= item[1] %>;
  proxy_ignore_headers "Set-Cookie";
  proxy_hide_header "Set-Cookie";
  include "/etc/nginx/includes/mconf-lb-node.conf";
}
<% end %>
