<%= node["mconf-lb"]["deploy_base"] %>/shared/log/*.log {
         size 300M
         copytruncate
         rotate 30
         compress
         missingok
         notifempty
         su <%= node["mconf-lb"]["user"] %> <%= node["mconf-lb"]["app_group"] %>
}
